# 独立 conda 环境，勿与 base / lite_ros2_env 混用
# Python 本体走 conda，其余依赖走 pip（见 requirements.txt）
name: lite_agilex_api
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r requirements.txt
